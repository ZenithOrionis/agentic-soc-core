[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "e2e: requires the Docker Compose stack to be running"
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

